# Copy this file to .env and fill in your values. Do not commit .env.

# --- Email (SMTP) - required for OTP, forgot password, and notifications ---
# Leave blank to disable; OTP will be shown in flash messages instead.
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_password
MAIL_USE_TLS=true
MAIL_FROM=your_email@gmail.com

# --- Optional: database (defaults to SQLite if not set) ---
# DATABASE_URL=sqlite:///placement.db
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# --- Optional: app ---
# SECRET_KEY=your-secret-key
# ENVIRONMENT=development
